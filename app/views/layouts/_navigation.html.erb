<nav class="navbar navbar-expand-lg bg-light">
  <div class="container-fluid">
    <%= link_to "PICTURES LIBRARY", root_path, class: "navbar-brand", id: "logo" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <% if current_user %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <% if current_user.admin? %>
            <li class="nav-item">
              <%= link_to "Users", users_path, class: "nav-link" %>
            </li>
          <% end %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              Pictures
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Create new picture", new_picture_path, class: "dropdown-item"%></li>
              <% if current_user.admin? %>
                <li><%= link_to "View my pictures", user_path(current_user), class: "dropdown-item"%>
                <li><%= link_to "View all pictures", pictures_path, class: "dropdown-item"%></li>
              <% else %>
                <li><%= link_to "View my pictures", user_path(current_user), class: "dropdown-item"%>
              <% end %>
            </ul>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= "(Admin)" if current_user.admin? %> Profile [<%= current_user.username %>]
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "View your profile",  user_path(current_user), class: "dropdown-item" %></li>
              <li><%= link_to "Edit Account", edit_user_registration_path(current_user), class: "dropdown-item" %></li>
            </ul>
          </li>
        </ul>
      <% end %>
      <ul class="navbar-nav">
        <% if current_user %>
          <li class="nav-item"> <%= button_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn-small btn-primary mx-2" %></li>
        <% else %>
          <li class="nav-item"> <%= link_to "Sign Up", new_user_registration_path, class: "nav-link"  %></li>
          <li class="nav-item"> <%= link_to "Log In", new_user_session_path, class: "nav-link"  %></li>
        <% end %>
      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>